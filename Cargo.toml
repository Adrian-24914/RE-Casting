[package]
name = "re-casting"
version = "0.1.0"
edition = "2021"

[dependencies]
image = { version = "0.25", default-features = false, features = ["png"] }
nalgebra-glm = "0.18.0"
minifb = "0.26.0"

[profile.dev]
debug = 0

[profile.release]
debug = 0

